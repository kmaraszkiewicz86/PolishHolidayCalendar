<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PolishHolidayCalendar.Presentation.Views.CalendarStartPage"
             xmlns:behaviors="clr-namespace:Kmaraszkiewicz86.Maui.Behaviors;assembly=Kmaraszkiewicz86.Maui.Behaviors"
             Title="CalendarStartPage">

    <ContentPage.Behaviors>
        <behaviors:EventToCommandBehavior EventName="Appearing" Command="{Binding LoadCommand}" />
    </ContentPage.Behaviors>

    <VerticalStackLayout>
        <Label 
            Text="{Binding SomeTextProperty}"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />


        <CollectionView
    ItemsSource="{Binding Months}"
    ItemsLayout="VerticalList"
    VerticalScrollBarVisibility="Always">

            <CollectionView.ItemTemplate>
                <DataTemplate>

                    <VerticalStackLayout Padding="12" Spacing="8">

                        <!-- Nazwa miesiąca -->
                        <Label
                    Text="{Binding MonthTitle}"
                    FontSize="18"
                    FontAttributes="Bold"
                    HorizontalOptions="Center" />

                        <!-- Siatka dni -->
                        <Grid
                    ColumnDefinitions="*,*,*,*,*,*,*"
                    RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto">

                            <!-- dni tygodnia -->
                            <Label Grid.Column="0" Text="Pn" />
                            <Label Grid.Column="1" Text="Wt" />
                            <Label Grid.Column="2" Text="Śr" />
                            <Label Grid.Column="3" Text="Cz" />
                            <Label Grid.Column="4" Text="Pt" />
                            <Label Grid.Column="5" Text="Sb" />
                            <Label Grid.Column="6" Text="Nd" />

                            <!-- dni miesiąca -->
                            <BindableLayout.ItemsSource>
                                <Binding Path="Days" />
                            </BindableLayout.ItemsSource>

                            <BindableLayout.ItemTemplate>
                                <DataTemplate>
                                    <Label
                                Text="{Binding Date.Day}"
                                HorizontalTextAlignment="Center"
                                VerticalTextAlignment="Center" />
                                </DataTemplate>
                            </BindableLayout.ItemTemplate>

                        </Grid>
                    </VerticalStackLayout>

                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </VerticalStackLayout>
</ContentPage>